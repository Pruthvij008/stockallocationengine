# Base URL of the Express backend.
# Local: http://localhost:8080  |  On Render this is set automatically via render.yaml
# (a bare hostname is fine — the app prepends https:// itself).
VITE_API_URL=http://localhost:8080
